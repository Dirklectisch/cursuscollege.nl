# Public name of this content type
name: Testimonials

# TODO: explain
slug: testimonials

# Explanation for the backoffice
description: Een testimonial voor de cursus

# Default field (e.g title)
label_field_name: quote

# Order of entries
order_by: manually # default: manually, also available: created_at or any field slug

# Order direction
# order_direction: asc # default: asc, also available: desc

# Display entries grouped by <field> in the backoffice
# group_by: <your field>

# Activate public 'create' API (e.g for a contact form)
# public_submission_enabled: false

# Emails to be notified on new entries using the public API
# public_submission_accounts: ['john@acme.net']

# Describe each field. The name shoud always be an underscored string
fields: 
- quote: # Name of the field
    label: Quote
    type: string
    required: true
    hint: De testimonial quote
    localized: false

- author: # Name of the field
    label: Author
    type: string
    required: false
    hint: De auteur van de quote
    localized: false

- course: # Name of the field
    label: Course # Human readable name of the field
    type: belongs_to
    required: true
    localized: false
    class_name: courses
